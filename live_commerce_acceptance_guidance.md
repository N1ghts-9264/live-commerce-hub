# 直播电商课程设计验收指导

适用课程：信息系统分析与设计  
适用项目：直播电商数字化运营管理系统  
本地项目：`/Users/jingxing/workspace/codex/course/live-commerce-hub`  
生成目的：把课程中关于信息系统分析与设计的要求，转化为 `live-commerce-hub` 项目的验收清单、优化方向和 Codex 检查提示词。

## 1. 验收定位

本项目不应只被展示为一个能登录、能点菜单、能显示图表的前后端应用，而应被验收为一个完整的信息系统分析与设计课程成果。验收时需要同时证明四件事：

1. 业务问题明确：系统解决直播电商企业从选品、采购、直播、订单、库存、售后到运营复盘的数据割裂与经验决策问题。
2. 分析模型完整：需求、DFD、数据字典、CRUD、Use Case、典型用例、领域对象能够互相支撑。
3. 设计落地清楚：三层架构、MVC、数据库、接口、权限、外部平台接入和 UI 任务路径能够对应到代码。
4. 功能可现场验证：每个测试台都有角色账号、操作路径、输入数据、预期输出和异常情况。

老师在课程中反复强调，课程设计不能停留在“功能清单”或“页面演示”，而要说明业务数据如何进入系统、如何被处理、如何存储、如何反馈给管理决策。直播电商项目尤其要讲清平台数据接入、直播实时数据时间维度、选品来源和新品冷启动逻辑。

## 2. 课程中的信息系统设计要点

### 2.1 需求与系统边界

- 需求来自业务活动、岗位职责、数据流转和管理痛点，不是用户随口提出的功能愿望。
- 需求获取需要同时覆盖开放式问题和封闭式问题：前者用于理解痛点和目标，后者用于确认字段、规则、频率、权限、异常处理和输出格式。
- 功能性需求说明系统必须完成什么行为；非功能性需求说明性能、安全、可维护性、易用性、兼容性等质量约束。
- 系统边界必须清晰。直播电商项目中，消费者、供应商、直播平台、支付平台、物流平台都可以是外部实体或数据来源，但不一定都是本系统的内部操作用户。

对本项目的验收含义：

- `客户/消费者` 应被定义为订单、互动、行为数据的来源，而不是默认登录本后台系统的操作员。
- `直播平台` 如抖音、快手、淘宝直播应作为外部系统或接口来源，向系统输入订单、评论、在线人数、商品曝光、转化等数据。
- `供应商/商家` 可以提供候选商品、价格、促销政策、库存和履约能力，是选品流程的起点之一。

### 2.2 DFD 与数据建模

DFD 的重点是说明数据如何进入系统、离开系统、被处理、被存储。验收时要检查：

- 上下文图只保留系统整体、外部实体和系统输入输出。
- 零层图把系统拆成主要处理过程，并加入数据存储和过程间数据流。
- 子图展开后与上层保持平衡，不能丢失上层输入输出。
- 外部实体不能直接连数据存储，数据存储不能直接连数据存储，处理过程不能只有输入没有输出或只有输出没有输入。
- 数据流命名应是业务数据，如 `直播实时指标`、`候选商品清单`、`采购建议`、`订单明细`，而不是泛泛的 `信息`、`数据`。

对本项目建议的核心数据存储：

- `D1 员工与权限`：Employee、Role、Permission、EmployeeRole、RolePermission。
- `D2 用户与行为`：User、UserBehaviorStat。
- `D3 商品与 SKU`：Product、SKU。
- `D4 供应商与采购`：Supplier、PurchaseOrder、PurchaseSuggestion。
- `D5 库存`：Inventory。
- `D6 直播场次与主播`：LiveSession、Anchor、AnchorPerformance。
- `D7 订单与售后`：Order、AfterSale。
- `D8 互动与实时监控`：InteractionLog。
- `D9 选品与商品表现`：ProductPerformance。
- `D10 报告与接口日志`：OperationReport、InterfaceLog。

### 2.3 Use Case 与参与者

用例从系统外部观察功能，描述参与者为了实现目标与系统发生的交互。不要把内部类、数据库表、算法模块当作用例参与者。

本项目建议保留的参与者：

- 管理层：查看驾驶舱、运营报告、主播绩效、选品结果。
- 运营人员：管理直播场次、监控直播、维护商品、查看选品分析、处理售后。
- 采购人员：查看选品结果、生成采购建议、维护采购单、跟踪供应商。
- 仓储人员：查看库存、处理入库出库、维护库存预警。
- 主播：查看直播监控、接收话术建议、执行直播脚本。
- 系统管理员：维护账号、角色、权限和基础配置。
- 外部实体：消费者、供应商、直播平台、支付平台、物流平台。

需要避免的错误：

- 把消费者画成后台系统的普通登录角色，除非项目明确包含面向消费者的购物前台。
- 把 `选品算法`、`数据库`、`AI 模型` 画成参与者。它们是系统内部模块或服务。
- 用例只写 `商品管理`、`订单管理` 这种模块名，而不说明目标、前置条件、主流程和异常流程。

### 2.4 架构与系统设计

课程中三层架构的核心价值是降低耦合：展示层负责界面，业务逻辑层负责业务规则和流程，数据层负责持久化和一致性。

本项目已经基本符合三层思路：

- 展示层：`client/src/pages`、`client/src/components`，基于 Vue 3、TypeScript、Pinia、Vue Router、Axios。
- 业务逻辑层：`server/src/routes`、`server/src/services`，基于 Express、TypeScript、JWT、业务服务。
- 数据层：`server/src/db`，基于 Knex、MSSQL、迁移和种子数据。

验收时应说明：

- 前端页面不直接访问数据库，而是通过 Axios 调用 REST API。
- 后端路由负责接口契约，服务层负责选品、直播模拟、采购建议、主播绩效等业务逻辑。
- 数据库迁移定义 24 张表，覆盖用户、员工、权限、供应商、商品、SKU、库存、采购、主播、直播、话术、订单、互动、售后、绩效、报告和接口日志。

### 2.5 接口与集成

课程中接口被定义为屏蔽实现细节的合约。调用方应只关心路径、参数、鉴权、返回格式和错误语义。

直播电商项目必须特别说明外部平台接口：

- 直播平台接口：直播间在线人数、互动评论、商品点击、成交、退款、流量来源。
- 电商订单接口：订单、支付状态、优惠、SKU、用户平台 ID。
- 物流接口：发货状态、运输状态、签收状态。
- 供应商接口或导入：候选商品、成本价、建议售价、促销支持、库存、供货周期。
- AI 服务接口：话术生成、互动情绪分析、选品顾问报告、新品评估。

当前项目已有 `InterfaceLog` 表，可以用于展示外部接口调用日志。验收时建议准备至少一组“平台数据接入”的说明：即使当前是模拟数据，也要说明真实系统中会通过接口日志记录平台、接口名、请求时间、状态、数据量和错误信息。

### 2.6 UI 与交互验证

UI 不只是美观，还是人机接口。验收时重点看任务路径是否自然：

- 管理层打开系统后能快速看到 GMV、订单量、转化率、库存预警和主播排行。
- 运营人员能从直播场次进入直播监控，看到在线人数、弹幕、订单、情绪分析和话术建议。
- 采购人员能从选品分析进入采购建议，再检查库存和采购单。
- 仓储人员能直接定位库存预警和采购到货。
- 主播能看到当前直播、话术和观众反馈，不需要理解后台复杂配置。

## 3. 第 11 节课堂汇报与老师点评要点

第 11 节主要是小组系统分析报告汇报和老师点评。与本项目最相关的要点如下。

### 3.1 最终验收形式

老师提到第 16 周最终展示会按小组设置 3 到 4 个测试台，由助教按小组准备的测试报告、测试单或测试大纲进行验证。每个测试台应放置不同角色或业务场景。

对本项目的直接要求：

- 必须准备测试报告或测试大纲，不能只临场口头演示。
- 每个测试台要有角色账号、测试目标、操作步骤、预期结果和异常处理。
- 测试台应覆盖管理层、运营/主播、采购/仓储、系统管理员等不同角色，而不是所有人都用管理员账号点页面。

### 3.2 汇报内容重点

第 11 节前后课程要求各组展示：

- 需求分析。
- 数据流分析，包括上下文图、零层图、数据字典。
- Use Case 与典型用例。
- 初步领域设计。
- 项目进展和系统原型。

对本项目的验收含义：

- 不能只展示运行效果，还要能把每个主要页面映射回 DFD、用例和数据库表。
- 讲选品、直播监控、采购、库存、售后时，应说明输入、处理、输出和数据存储。
- 讲 AI 功能时，应说明它是业务逻辑层服务，而不是替代需求分析和系统设计。

### 3.3 对直播电商组的关键点评

老师对直播电商类项目提出了几个高价值问题：

1. 客户是否真的操作本系统：消费者会下单、评论、浏览，但通常不登录企业内部运营后台。Use Case 中要区分“数据来源”和“后台操作角色”。
2. 如何对接现有直播平台：如果销售发生在抖音、快手、淘宝等平台，就要定义平台数据如何进入本系统。
3. 选品从哪里开始：不能说“看到网上什么好卖就卖什么”。系统要定义候选商品池。
4. 新商品如何处理：历史销量只能评价老品，新品没有历史销售数据，需要推广试验、供应商支持和相似品数据。
5. 直播实时数据是否有时间维度：一场直播总体数据不够，应考虑按分钟甚至秒级切片记录在线人数、热度、评论、订单和转化变化。
6. 主播绩效评价很重要：从选品到销售，主播表现是直播监测管理中的重要设计点。

这些点评应被作为最终验收的重点风险项，而不是附带说明。

## 4. 与 `live-commerce-hub` 当前架构的映射

### 4.1 功能模块映射

| 课程设计交付物 | 当前项目实现 | 验收关注点 |
|---|---|---|
| 需求与角色 | README 中有 6 个员工账号和 RBAC 说明 | 是否区分内部角色与外部实体 |
| 数据总览 | `DashboardPage.vue`、`/api/dashboard` | KPI 是否服务管理决策 |
| 直播监控 | `MonitorPage.vue`、SSE、`liveSimulator.ts` | 是否能解释实时指标和弹幕分析 |
| 商品管理 | `ProductsPage.vue`、`/api/products` | 商品、SKU、供应商关系是否清楚 |
| 主播管理 | `AnchorsPage.vue`、主播绩效接口 | 是否能支撑主播评价 |
| 采购管理 | `PurchasingPage.vue`、采购建议服务 | 是否能从选品和库存推导采购 |
| 库存管理 | `InventoryPage.vue`、库存预警 | 是否支撑仓储测试台 |
| 选品分析 | `SelectionPage.vue`、`selectionEngine.ts` | 老品评分、新品冷启动、候选池定义 |
| 话术管理 | `ScriptsPage.vue`、LLM 服务 | 是否有预览、保存和直播关联 |
| 直播场次 | `LiveSessionsPage.vue`、`/api/live-sessions` | 排期、编辑、模拟启动是否能演示 |
| 售后工单 | `AfterSalePage.vue`、`/api/after-sales` | 状态流转是否闭环 |
| 运营报告 | `ReportsPage.vue`、`/api/reports` | 报告是否基于数据对象和统计周期 |
| 接口日志 | `InterfaceLog`、`/api/interface-logs` | 是否能说明平台接入和异常记录 |

### 4.2 数据库表映射

当前迁移文件 `server/src/db/migrations/001_create_all_tables.ts` 创建了 24 张表，覆盖课程所需的主要数据对象。验收时建议把表分成业务域说明：

- 组织与权限域：Employee、Role、Permission、EmployeeRole、RolePermission。
- 用户与平台用户域：User。
- 商品供应链域：Supplier、Product、SKU、Inventory、PurchaseOrder、PurchaseSuggestion。
- 直播运营域：Anchor、LiveSession、Script、InteractionLog、UserBehaviorStat。
- 交易售后域：Order、AfterSale。
- 绩效分析域：AnchorPerformance、ProductPerformance、KPIIndicator、OperationReport。
- 系统集成域：InterfaceLog。

需要补充说明的点：

- `ProductPerformance` 当前没有独立时间字段，只关联直播场次；如果要做趋势分析，应从 `LiveSession.start_time/end_time` 或新增 `statistical_time` 解释时间窗口。
- `UserBehaviorStat.statistical_time` 和 `InteractionLog.interaction_time` 可以支撑实时切片，应在报告中明确用于按分钟或按秒统计。
- `InterfaceLog` 目前可证明有接口日志设计，但需要演示或说明平台数据接入过程。

### 4.3 当前已知缺口

根据现有验证计划和代码观察，建议在最终验收前重点补齐或至少在报告中解释：

- 直播场次创建/编辑仍是待完整验证项。
- 选品分析的排名、关联推荐、数字顾问报告仍需前端完整验收。
- 运营报告查看仍需验证。
- 售后工单状态流转仍需验证。
- `selectionEngine.ts` 中趋势评分使用 `60 + Math.random() * 40` 作为占位，应替换为基于时间窗口的确定性趋势计算，或在验收说明中承认这是待优化实现。
- 后端已有 `/api/selection/coldstart/:productId`，前端 API 也有 `selectionAPI.coldstart`，但 `SelectionPage.vue` 没有明显入口，应增加 UI 触发或准备 API 测试。
- 直播模拟会实时插入订单和互动日志，但未显式写入按分钟的 `UserBehaviorStat`，应说明实时切片从 SSE 状态、InteractionLog 和 UserBehaviorStat 如何组合。

## 5. “新商品如何选取”专项最佳实践

这是直播电商项目的验收重点。推荐在报告和演示中把选品分成“候选池、评分、试播、复盘、采购”五步。

### 5.1 候选商品池从哪里来

不要把选品描述成“网上看到什么火就卖什么”。合理候选池应来自：

- 合作供应商主动推品：供应商提交商品、成本价、建议售价、促销支持、库存和供货周期。
- 自有商品池：企业已有商品、历史商品、可复购商品、库存待清商品。
- 直播平台商品池：抖音、快手、淘宝等平台可带货商品池或联盟商品池。
- 商家活动池：品牌方阶段性促销、新品首发、季节性活动。
- 用户需求池：评论、搜索、收藏、历史购买、售后反馈中体现的需求。

系统中可映射到：

- Supplier 提供供应商信息。
- Product/SKU 表示候选或在售商品。
- Inventory 表示库存与安全库存。
- InterfaceLog 表示平台或供应商接口导入记录。
- ProductPerformance 表示历史表现。

如果要进一步优化，建议新增或在报告中定义逻辑对象 `CandidateProductPool`：

| 字段 | 含义 |
|---|---|
| candidate_id | 候选商品编号 |
| product_id | 对应商品，可为空，表示尚未正式入库 |
| source_type | 供应商推品/平台商品池/自有商品/活动商品/用户需求 |
| source_ref | 来源平台或供应商引用 |
| campaign_support | 供应商给出的推广价、坑位费、佣金、优惠券等支持 |
| expected_margin | 预计毛利 |
| supply_capacity | 可供货量 |
| trial_status | 待评估/试播中/通过/淘汰 |
| review_reason | 通过或淘汰原因 |

### 5.2 老品与新品评分分开

老品可以使用历史数据：

- 转化率、点击率、互动热度。
- 销量、GMV、退款率。
- 毛利率、价格带。
- 库存周转和缺货风险。
- 主播匹配度和历史场次表现。

新品不能依赖自身历史销量，应使用冷启动逻辑：

- 供应商给出的推广价格、佣金、优惠力度和库存保障。
- 同品类、相似价格带、相似品牌或相似卖点商品的历史表现。
- 平台趋势、类目热度、季节性和活动档期。
- 小流量试播或短时段试推的数据。
- 直播间评论、点击、加购、下单转化的早期信号。
- 采购风险：最小起订量、供货周期、退货风险、售后风险。

当前项目已有 `coldStart(productId)`，逻辑包括相似商品、品类趋势和 LLM 评估。验收时建议这样讲：

> 对历史商品，系统使用转化、利润、热度、趋势、质量五维评分；对新品，系统先从供应商或平台候选池进入冷启动流程，使用相似商品表现、品类趋势、供应商支持和 AI 评估生成初始信心分，再通过试播数据逐步替代估计值。

### 5.3 新品选品流程

建议在报告中画成如下流程：

1. 候选进入：供应商、平台商品池、自有商品池或活动池提交候选商品。
2. 基础筛选：检查类目、成本价、建议售价、毛利率、供货周期、库存、资质和售后风险。
3. 相似品匹配：按类目、价格带、品牌、卖点匹配已有商品表现。
4. 冷启动评分：综合相似品转化、品类趋势、供应商促销支持、库存保障和 AI 评估。
5. 试播推广：选择低风险直播场次进行短时段展示，记录点击、评论、加购、订单、退款意向。
6. 数据回流：把试播数据写入 ProductPerformance、InteractionLog、UserBehaviorStat。
7. 决策输出：进入正式推广、继续观察、调整价格/话术、淘汰。
8. 采购联动：通过 PurchaseSuggestion 生成建议采购量，避免爆品缺货或滞销积压。

### 5.4 新品验收用例

| 测试项 | 操作 | 预期结果 |
|---|---|---|
| 候选来源 | 选择一个供应商提供的新品 | 能说明供应商、成本价、售价、供货周期和促销支持 |
| 冷启动分析 | 调用 `/api/selection/coldstart/:productId` | 返回相似商品、LLM 评估、探索加权、置信度和估计分 |
| 试播验证 | 在直播监控中推广该商品 | 产生互动、订单和实时指标 |
| 数据回流 | 查看 InteractionLog、Order、ProductPerformance 或监控图表 | 能说明新品早期信号如何进入后续评分 |
| 采购联动 | 根据试播结果生成采购建议 | 能解释建议采购量与库存风险 |

## 6. 第 16 周测试台设计

建议准备 4 个测试台。每个测试台都要有账号、场景、步骤和预期结果。

### 测试台 A：管理层驾驶舱与报告

账号：`EMP001 / 123456` 或 `EMP006 / 123456`  
角色：管理层/系统管理员  
目标：验证管理层能通过系统掌握运营状态。

| 步骤 | 操作 | 预期结果 |
|---|---|---|
| A1 | 登录系统进入数据总览 | KPI 卡片、趋势图、主播排行显示数据 |
| A2 | 查看主播管理和主播绩效 | 能看到主播基础信息、能力雷达或绩效数据 |
| A3 | 查看运营报告 | 报告列表可访问，报告内容能说明统计周期 |
| A4 | 查看接口日志 | 能说明外部平台数据接入记录 |
| A5 | 解释课程模型 | 把驾驶舱数据对应到订单、直播、商品、主播、用户行为等数据存储 |

### 测试台 B：运营人员与主播直播监控

账号：`EMP002 / 123456`、`EMP005 / 123456`  
角色：运营人员/主播  
目标：验证直播过程中的实时监控、互动分析和话术辅助。

| 步骤 | 操作 | 预期结果 |
|---|---|---|
| B1 | 进入直播场次，选择一个已排期场次 | 能查看直播标题、主播、平台和类目 |
| B2 | 启动直播模拟 | 在线人数、订单、GMV、弹幕开始实时变化 |
| B3 | 观察弹幕和情绪分析 | InteractionLog 写入互动记录，页面显示情绪或语义反馈 |
| B4 | 触发话术建议 | 当价格或负面情绪出现时，系统给出话术建议 |
| B5 | 解释时间维度 | 说明 interaction_time、statistical_time 或 SSE 指标如何支持分钟级/秒级切片 |

### 测试台 C：采购、选品与库存闭环

账号：`EMP003 / 123456`、`EMP004 / 123456`  
角色：采购人员/仓储人员  
目标：验证从选品到采购再到库存的业务闭环。

| 步骤 | 操作 | 预期结果 |
|---|---|---|
| C1 | 进入选品分析 | 显示商品排名、五维评分和顾问报告 |
| C2 | 查看关联推荐 | 选择商品后能看到共同购买或关联商品 |
| C3 | 测试新品冷启动 | 调用 coldstart API 或通过新增 UI 入口触发 |
| C4 | 进入采购管理 | 能查看采购单、状态流转和采购建议 |
| C5 | 进入库存管理 | 能查看库存预警、仓库和 SKU 库存 |
| C6 | 解释选品来源 | 明确候选商品来自供应商、平台商品池、自有商品池或活动池 |

### 测试台 D：权限、售后与系统边界

账号：`EMP001` 至 `EMP006` 交叉测试  
角色：系统管理员、运营、采购、仓储、主播  
目标：验证权限隔离、售后流程和系统边界。

| 步骤 | 操作 | 预期结果 |
|---|---|---|
| D1 | 6 个账号分别登录 | 不同角色看到不同菜单 |
| D2 | 用仓储账号直接访问主播管理 URL | 应被拦截或无法操作 |
| D3 | 进入售后工单 | 能查看售后类型、问题描述、状态和退款金额 |
| D4 | 流转售后状态 | 状态变化符合业务规则 |
| D5 | 说明消费者边界 | 消费者是订单和互动数据来源，不是后台操作用户 |

## 7. 课程交付物验收清单

### 7.1 需求分析

- [ ] 项目背景说明直播电商企业的真实管理痛点：数据分散、选品经验化、直播过程不可控、库存与采购脱节、售后复盘慢。
- [ ] 明确系统边界：本系统是企业内部运营管理后台，不是消费者购物前台。
- [ ] 明确内部角色：管理层、运营、采购、仓储、主播、系统管理员。
- [ ] 明确外部实体：消费者、供应商、直播平台、支付平台、物流平台、AI 服务。
- [ ] 功能需求覆盖选品、商品、主播、直播、订单、库存、采购、售后、报告、权限。
- [ ] 非功能需求覆盖响应速度、权限安全、数据一致性、可维护性、易用性、接口可追踪。

### 7.2 DFD 与数据字典

- [ ] 上下文图展示外部实体与系统之间的数据交换。
- [ ] 零层图至少包含选品管理、直播监控、订单处理、库存采购、售后处理、报表分析、权限管理。
- [ ] 每个处理过程有输入、输出、读写数据存储。
- [ ] 数据字典定义主要数据流、数据元素、数据结构和数据存储。
- [ ] CRUD 矩阵覆盖 Product、SKU、Inventory、Order、LiveSession、InteractionLog、AfterSale、PurchaseOrder、OperationReport。
- [ ] 明确直播实时数据的时间粒度。

### 7.3 用例与典型用例

- [ ] 用例图区分内部角色和外部系统。
- [ ] 至少写 4 个典型用例：直播监控、智能选品、新品冷启动、库存采购联动、售后处理。
- [ ] 每个典型用例包含前置条件、主成功场景、备选流程、异常流程和后置条件。
- [ ] 用例能映射到 API、页面和数据库表。

### 7.4 系统设计

- [ ] 说明 Vue 前端、Express 后端、MSSQL 数据库的三层架构。
- [ ] 说明前端页面、后端路由、服务层和数据库表之间的对应关系。
- [ ] 说明 JWT、RBAC 和角色菜单控制。
- [ ] 说明 SSE 直播实时推送机制。
- [ ] 说明 AI 服务在话术、情绪分析、选品报告、新品评估中的边界。
- [ ] 说明外部平台接口的输入输出、鉴权、异常和日志。

### 7.5 代码与运行验收

- [ ] 数据库迁移能创建 24 张表。
- [ ] 种子数据可支撑演示，且关键表数据量合理。
- [ ] 后端核心 API 可用，并返回稳定结构。
- [ ] 前端登录、菜单、页面切换正常。
- [ ] 4 个测试台按角色可完成操作。
- [ ] 选品、新品冷启动、直播监控、采购库存、售后报告均有演示路径。
- [ ] 对尚未完全实现的功能有清晰说明和优化方案。

## 8. 优化建议优先级

### P0：验收前必须能讲清

- 系统边界：消费者不是后台操作员，直播平台是外部数据源。
- 选品起点：候选商品来自供应商、平台商品池、自有商品池或活动池。
- 新品冷启动：新品不能只靠历史销售，需要相似品、品类趋势、供应商支持和试播数据。
- 实时数据时间维度：直播数据应按分钟或秒级切片，至少要说明当前字段如何支撑。
- 测试台：准备 3 到 4 个角色化测试台和测试单。

### P1：建议尽快补强

- 在 `SelectionPage.vue` 增加新品冷启动入口，调用 `selectionAPI.coldstart(productId)`。
- 将 `selectionEngine.ts` 的随机趋势分替换为确定性趋势计算，例如按近 7 天和前 7 天的 GMV、转化率、互动热度对比。
- 在直播模拟或定时任务中写入 `UserBehaviorStat`，形成可查询的实时指标切片。
- 增强 `InterfaceLog` 的演示数据，用于说明平台接口接入。
- 补齐直播场次创建/编辑、售后状态流转、运营报告查看的前端验收。

### P2：报告加分项

- 增加候选商品池对象或表设计。
- 增加选品评分解释页面，展示每个维度的分数来源。
- 增加主播与商品匹配度：主播类目专长、粉丝画像、历史转化。
- 增加异常场景：平台接口失败、库存不足、供应商延期、直播负面情绪突增。
- 增加模型可解释性：AI 生成内容必须保留输入数据、评分依据和人工确认入口。

## 9. 可直接给 Codex 的验收提示词

### 9.1 全项目架构检查

```text
请检查 /Users/jingxing/workspace/codex/course/live-commerce-hub，按“信息系统分析与设计”课程验收标准输出问题清单。
重点检查：
1. Vue 前端、Express 后端、MSSQL 数据库是否形成清晰三层架构。
2. 每个主要页面是否能映射到后端 API、服务逻辑和数据库表。
3. 角色权限是否能支撑管理层、运营、采购、仓储、主播、系统管理员的测试台。
4. 外部实体是否被正确建模：消费者、供应商、直播平台、支付平台、物流平台。
5. 直播实时数据是否有时间维度，是否能支持分钟级或秒级切片说明。
请按 P0/P1/P2 给出缺口和建议，不要只评价代码风格。
```

### 9.2 选品与新品冷启动专项检查

```text
请专项检查 live-commerce-hub 的选品模块，包括 server/src/services/selectionEngine.ts、server/src/routes/selection.ts、client/src/pages/SelectionPage.vue 和 client/src/api/index.ts。
请回答：
1. 当前老品评分使用了哪些指标，是否可解释。
2. 新品 coldstart 是否能解决“没有历史销量”的问题。
3. 候选商品池来源是否明确，是否能从供应商、平台商品池、自有商品池或活动池进入。
4. 前端是否提供冷启动入口；如果没有，请给出最小修改方案。
5. trendScore 是否存在随机占位；如果存在，请给出基于 ProductPerformance、LiveSession、Order、InteractionLog 的确定性替代方案。
输出结果请直接用于课程验收整改。
```

### 9.3 测试台与验收脚本生成

```text
请基于 live-commerce-hub 生成第 16 周课程验收测试台脚本。
要求：
1. 设计 4 个测试台：管理层驾驶舱、运营/主播直播监控、采购/仓储选品库存、权限/售后系统边界。
2. 每个测试台指定账号 EMP001-EMP006、测试目标、操作步骤、预期结果、异常情况。
3. 每个测试步骤都要映射到具体页面、API 或数据库表。
4. 特别加入“消费者不是后台操作员”“直播平台数据接入”“新品如何选取”“直播实时数据时间维度”的追问回答。
```

### 9.4 DFD 与数据字典补全

```text
请根据 live-commerce-hub 当前数据库迁移、路由和页面，帮我补全课程报告中的 DFD、数据字典和 CRUD 矩阵。
要求：
1. 给出上下文图的外部实体和输入输出数据流。
2. 给出零层图的主要处理过程、数据存储和数据流。
3. 给出 Product、SKU、Inventory、Order、LiveSession、InteractionLog、UserBehaviorStat、ProductPerformance、PurchaseOrder、AfterSale、InterfaceLog 的数据字典摘要。
4. 给出主要角色对核心数据对象的 CRUD 矩阵。
5. 检查是否存在 DFD 常见错误：外部实体直连数据存储、数据存储直连数据存储、处理过程无输入或无输出。
```

## 10. 汇报时推荐的回答口径

### 老师问：你们的客户是否进入系统？

推荐回答：

> 我们把消费者定义为外部实体和数据来源，而不是企业后台的内部操作用户。消费者在抖音、快手、淘宝等直播平台完成观看、评论、下单等行为，这些行为通过平台接口进入本系统，形成 User、Order、InteractionLog、UserBehaviorStat 等数据。本系统主要服务管理层、运营、采购、仓储、主播和系统管理员。

### 老师问：直播平台数据怎么进来？

推荐回答：

> 真实场景下通过平台 API 或数据导入接口接入直播间实时指标、订单、评论和商品表现。系统用 InterfaceLog 记录接口名、平台、请求时间、状态和数据量；业务数据分别落到 Order、InteractionLog、UserBehaviorStat、ProductPerformance 等表。当前演示环境使用直播模拟服务生成实时数据，但数据结构按平台接入方式设计。

### 老师问：你们的选品从哪里开始？

推荐回答：

> 选品从候选商品池开始，不是从全网随机搜索开始。候选池来自合作供应商推品、平台商品池、自有商品池、品牌活动池和用户需求反馈。系统先做基础过滤，再做老品评分或新品冷启动评分，最后结合试播数据和库存采购约束输出推荐清单。

### 老师问：新品没有历史销量，怎么选？

推荐回答：

> 新品进入冷启动流程。系统先检查供应商价格、促销支持、库存和供货周期，再匹配同品类、相似价格带、相似卖点商品的历史表现，结合品类趋势和 AI 评估生成初始分。随后通过小流量试播收集点击、互动、加购、订单和负面反馈，逐步用真实试播数据替代估计值。

### 老师问：直播实时数据有没有时间维度？

推荐回答：

> 有。互动数据使用 InteractionLog.interaction_time，用户行为统计使用 UserBehaviorStat.statistical_time，直播场次有 start_time 和 end_time。验收中我们按直播场次和时间窗口解释在线人数、互动热度、订单和转化变化。后续优化会把实时模拟指标定期落入 UserBehaviorStat，以支持更稳定的分钟级趋势分析。

## 11. 最终验收前建议完成的最小整改

1. 为选品页面增加“新品冷启动评估”按钮或弹窗，调用已有 `selectionAPI.coldstart(productId)`。
2. 把随机趋势分替换为确定性计算，至少使用最近直播场次与历史场次的 GMV、销量、转化率或互动热度对比。
3. 准备 4 张测试台测试单，按 EMP001-EMP006 分配角色。
4. 在报告中补一页“直播平台数据接入设计”，明确 InterfaceLog、InteractionLog、Order、UserBehaviorStat 的关系。
5. 在报告中补一页“新品选品流程”，明确候选池、冷启动、试播、复盘和采购联动。
6. 验证并记录直播场次创建/编辑、选品分析、运营报告、售后状态流转这几个当前待确认功能。

## 12. 资料来源

- 课程已整理笔记：`outputs/course-138010/信息系统分析与设计`
- 课程项目支持包：`outputs/course-138010/course_project_support_live_commerce`
- 第 11 节课堂转写与提纲：`outputs/course-138010/lessons/5762534`
- 项目代码：`/Users/jingxing/workspace/codex/course/live-commerce-hub`
- 项目验证计划：`/Users/jingxing/workspace/codex/course/live-commerce-hub/验证计划.md`
