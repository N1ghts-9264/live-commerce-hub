# 直播电商数字化运营管理系统

> **Live Commerce Digital Operations Management System**

一个全栈直播电商数字化运营管理平台，提供从选品分析、直播监控、主播管理到售后处理的全链路数字化管理能力。

## 系统功能模块

| 模块 | 功能说明 |
|------|---------|
| **仪表盘** | KPI 总览（GMV、订单量、转化率、库存预警）、近 30 天销售趋势图、TOP5 主播排行 |
| **直播监控** | 实时直播模拟（SSE 推送）、在线人数/订单/弹幕实时展示、AI 情绪分析、AI 智能话术推荐 |
| **商品管理** | 商品 CRUD、分类/状态筛选、排序搜索 |
| **主播管理** | 主播 CRUD、雷达图能力展示、多主播对比 |
| **话术管理** | AI 生成直播话术（DeepSeek LLM）、预览后保存 |
| **库存管理** | 多仓库库存追踪、低库存预警高亮 |
| **采购管理** | 采购单状态流（待审核→已审核→已发货→已收货）、低库存自动采购建议 |
| **直播场次** | 直播场次排期管理、关联主播 |
| **选品分析** | 多因子商品评分（转化率/利润率/热度/趋势/质量）、Jaccard 相似度推荐、LLM 新品冷启动评估 |
| **售后管理** | 售后工单管理、状态流转处理 |
| **运营报告** | 运营报告生成（LLM 内容生成） |
| **供应商管理** | 供应商 CRUD、评分追踪 |
| **订单管理** | 订单列表查看、用户/商品关联详情 |
| **权限管理** | RBAC 角色权限体系（6 种角色 × 20 项权限） |

## 技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| **前端框架** | Vue 3 + TypeScript | Composition API + `<script setup>` |
| **构建工具** | Vite 8 | 开发服务器端口 5173 |
| **状态管理** | Pinia 3 | 认证状态管理 |
| **路由** | Vue Router 4 | 懒加载路由 |
| **HTTP 客户端** | Axios | JWT 认证拦截器 |
| **图表** | Chart.js 4 + vue-chartjs | 折线图、饼图、雷达图 |
| **图标** | Lucide Vue Next | 现代图标库 |
| **后端框架** | Express 4 + TypeScript | RESTful API 端口 3000 |
| **数据库 ORM** | Knex 3 | 查询构建 + 迁移 + 种子数据 |
| **数据库** | Microsoft SQL Server | 本地 localhost:1433 |
| **认证** | JWT + bcryptjs | Bearer Token |
| **AI 大模型** | DeepSeek Chat（OpenAI 兼容）| 话术生成、选品建议、报告生成 |

---

## 小白也能跑起来 —— 完整安装指南

### 第一步：安装必备软件

在开始之前，请确保你的电脑安装了以下软件：

#### 1. Node.js（版本 18 或以上）

下载地址：https://nodejs.org

下载 LTS 版本，双击安装，一路"下一步"即可。

安装完成后，打开 **命令提示符**（Win+R 输入 `cmd` 回车），输入以下命令验证：

```bash
node -v
npm -v
```

如果都能看到版本号，说明安装成功。

#### 2. SQL Server 2022（Developer 版，免费）

下载地址：https://www.microsoft.com/zh-cn/sql-server/sql-server-downloads

往下滚动找到 **"Developer"** 版本，点击"立即下载"。

**安装步骤（关键！）：**

1. 双击安装包，选择 **"基本"** 安装类型
2. 一路点击"下一步"，等待安装完成
3. **重点：安装 SSMS（SQL Server Management Studio）**，安装完成后会弹出提示，点击安装 SSMS 即可
4. 打开 SSMS，连接到 `localhost`，使用 **SQL Server 身份验证**：
   - 登录名：`sa`
   - 密码：`123456`（你安装时设置的密码）

> **如果安装时没有设置 sa 密码，或忘记了密码：**
>
> 1. 打开 SSMS，使用 **Windows 身份验证** 连接
> 2. 在左侧"对象资源管理器"中，右键点击服务器名称 → "属性" → "安全性" → 选择 "SQL Server 和 Windows 身份验证模式"
> 3. 展开 "安全性" → "登录名" → 右键 `sa` → "属性" → 设置新密码
> 4. 右键服务器名称 → "重新启动"

#### 3. Git（用来上传到 GitHub）

下载地址：https://git-scm.com/download/win

下载后一路"下一步"安装即可。

---

### 第二步：克隆项目 & 安装依赖

如果你拿到了 GitHub 仓库地址，克隆到本地：

```bash
git clone https://github.com/N1ghts-9264/live-commerce-hub.git
cd live-commerce-hub
```

如果本项目已经在本地，直接在项目文件夹中打开命令提示符：

> 在项目文件夹地址栏输入 `cmd` 回车，或在文件夹空白处按 `Shift + 右键` → "在此处打开 PowerShell 窗口"

然后安装所有依赖：

```bash
# 安装根目录依赖
npm install

# 安装前端依赖
cd client
npm install
cd ..

# 安装后端依赖
cd server
npm install
cd ..
```

---

### 第三步：配置数据库连接

#### 1. 创建数据库

打开 **SSMS**，连接到你的 SQL Server，然后：

1. 右键左侧 "数据库" → "新建数据库"
2. 数据库名称输入：`live_commerce_hub`
3. 点击"确定"

#### 2. 配置环境变量

进入 `server` 文件夹，复制 `.env.example` 并重命名为 `.env`：

```bash
cd server
copy .env.example .env
```

然后用**记事本**打开 `.env` 文件，修改以下内容：

```env
# MSSQL 数据库连接
DB_HOST=localhost
DB_PORT=1433
DB_USER=sa
DB_PASSWORD=你的sa密码       # ← 把这里改成你的数据库密码

# JWT（可以不改，用默认值也行）
JWT_SECRET=live-commerce-hub-secret-key-change-in-production

# DeepSeek API（如果你没有 DeepSeek API Key，AI 功能会用不了，但其他功能不受影响）
LLM_API_KEY=你的API_Key      # ← 去 https://platform.deepseek.com 注册获取
LLM_BASE_URL=https://api.deepseek.com/v1
LLM_MODEL=deepseek-chat

# 服务器端口
PORT=3000
```

> 如果你没有 DeepSeek API Key，LLM 相关功能（AI 话术生成、选品分析报告等）会报错，但**不影响登录、商品管理、库存管理等其他所有功能的正常使用**。

---

### 第四步：初始化数据库（建表 + 造数据）

这一步会自动创建 24 张业务表。课堂验收建议使用稳定的验收数据集，数据量适中、可重复运行，便于现场演示和报告截图。

```bash
# 确保在项目根目录
cd live-commerce-hub

# 执行数据库迁移（建表）
npm run migrate

# 推荐：写入课堂验收数据
npm run seed:acceptance
```

**执行时间说明：**
- `npm run migrate`：约 5~10 秒，创建 24 张表
- `npm run seed:acceptance`：约 10~30 秒，写入可重复验收数据
  - 6 个岗位账号 + 6 个角色 + 20 项权限
  - 144 商品 + 432 SKU + 864 库存记录
  - 120 场直播 + 2,400 订单 + 4,800 条互动记录
  - 720 条商品表现 + 240 条用户行为统计
  - 160 条售后 + 180 条采购单 + 120 条接口日志

> 课堂汇报、现场验收和报告截图优先使用 `npm run seed:acceptance`。它不依赖 LLM API，也不依赖随机生成，连续运行结果一致。

#### 可选方案 A：运行原始随机种子

如果希望生成更大的随机演示数据，可以执行：

```bash
npm run seed
```

该方式数据量更大，但每次运行结果会变化，不适合需要稳定截图和复现实验结果的验收场景。

> 如果 seed 过程中出现报错，通常是数据库连接配置问题，检查 `.env` 文件的数据库密码是否正确。

#### 可选方案 B：直接导入预置 SQL 文件

如果你不想运行 seed，或者想让数据与项目作者的**完全一致**，可以直接导入预置的 SQL 文件：

```bash
# 在项目根目录执行
sqlcmd -S localhost,1433 -U sa -P 你的sa密码 -d live_commerce_hub -i server\db\export.sql
```

这个 SQL 文件已经包含了完整的建表 + 数据（约 28 万条），**一步到位**，无需再执行 `npm run migrate` 和 `npm run seed`。

> 三种方式二选一即可：
> - **课堂验收推荐**：`npm run migrate` + `npm run seed:acceptance`
> - **随机大数据演示**：`npm run migrate` + `npm run seed`
> - **作者数据库快照**：`sqlcmd -i server\db\export.sql`

#### 验证命令

```bash
# 验收逻辑测试 + 前后端构建
npm run test:acceptance
npm run build
```

---

### 第五步：启动项目

```bash
# 确保在项目根目录
npm run dev
```

这个命令会同时启动：
- **前端**：http://localhost:5173
- **后端 API**：http://localhost:3000

浏览器会自动打开前端页面（或手动访问 `http://localhost:5173`）。

---

### 第六步：登录测试

系统预置了 6 个测试账号，对应不同角色：

| 员工编号 | 密码 | 角色 | 权限范围 |
|---------|------|------|---------|
| `EMP001` | `123456` | 管理层 | 全部权限 |
| `EMP002` | `123456` | 运营人员 | 运营相关模块 |
| `EMP003` | `123456` | 采购人员 | 采购相关模块 |
| `EMP004` | `123456` | 仓储人员 | 库存相关模块 |
| `EMP005` | `123456` | 主播 | 主播相关模块 |
| `EMP006` | `123456` | 系统管理员 | 全部权限 |

**推荐用 EMP001 登录**，可以看到全部功能。

---

## 项目结构

```
live-commerce-hub/
├── client/                          # 前端 Vue 3 项目
│   └── src/
│       ├── components/              # 可复用组件（侧边栏、表格、分页等）
│       ├── pages/                   # 页面组件（11 个功能页面）
│       ├── stores/                  # Pinia 状态管理
│       ├── types/                   # TypeScript 类型定义
│       ├── utils/                   # 工具函数
│       ├── App.vue                  # 根组件
│       └── main.ts                  # 入口文件
├── server/                          # 后端 Express 项目
│   └── src/
│       ├── db/
│       │   ├── migrations/          # 数据库迁移文件（建表）
│       │   └── seeds/               # 种子数据文件
│       ├── middleware/              # 中间件（认证等）
│       ├── routes/                  # API 路由（17 个模块）
│       ├── app.ts                   # Express 应用配置
│       └── index.ts                 # 服务器入口
├── package.json                     # 根 package.json（统一脚本）
└── README.md                        # 本文件
```

---

## 常用命令速查

| 命令 | 说明 |
|------|------|
| `npm run dev` | 同时启动前端 + 后端 |
| `npm run dev:server` | 仅启动后端 |
| `npm run dev:client` | 仅启动前端 |
| `npm run migrate` | 执行数据库迁移（建表） |
| `npm run seed` | 插入种子测试数据 |
| `npm run build` | 构建前端生产版本 |

---

## 常见问题

### Q: 启动报错 "用户 'sa' 登录失败"

A: 数据库密码不对，检查 `server/.env` 中 `DB_PASSWORD` 是否正确。

### Q: 执行 seed 太慢了

A: 种子数据包含约 28 万条记录，大数据量插入需要 2~5 分钟是正常的，请耐心等待。

### Q: AI 功能报错（话术生成、选品分析等）

A: 需要配置 DeepSeek API Key。去 https://platform.deepseek.com 注册获取（有免费额度），填入 `server/.env` 的 `LLM_API_KEY`。

### Q: 端口被占用

A: 如果 3000 或 5173 端口被占用：
- 修改 `server/.env` 的 `PORT` 为其他端口（如 3001）
- 同时修改 `client/vite.config.ts` 中 proxy 的 target 地址

### Q: npm install 报错

A: 尝试删除 `node_modules` 文件夹后重新安装：
```bash
rmdir /s node_modules
rmdir /s client\node_modules
rmdir /s server\node_modules
npm install
cd client && npm install && cd ..
cd server && npm install && cd ..
```

---

## 系统测试指南

### 为什么每个账号都要测试？

系统采用 **RBAC（基于角色的访问控制）**，不同角色登录后看到的**菜单不同、能操作的功能不同**。用管理员账号测不到权限隔离的问题，所以**6 个账号都需要测试**。

### 角色权限速查

| 角色 | 可见菜单数量 | 核心模块 |
|------|:----------:|---------|
| 管理层 (EMP001) | 11 个（全部） | 全部模块 + 数据分析 |
| 系统管理员 (EMP006) | 11 个（全部） | 全部模块 + 系统管理 |
| 运营人员 (EMP002) | 9 个 | 直播监控、场次、选品、商品、主播、脚本、售后、报告 |
| 采购人员 (EMP003) | 5 个 | 选品、商品、采购、库存、售后 |
| 仓储人员 (EMP004) | 2 个 | 库存管理、采购管理 |
| 主播 (EMP005) | 3 个 | 直播监控、脚本管理、数据分析 |

### 账号 × 模块测试矩阵

> 打勾 ✅ 表示该角色应该能访问该模块；打叉 ❌ 表示不应看到该菜单；横线 — 表示不适用。

| 测试模块 | EMP001 管理层 | EMP002 运营 | EMP003 采购 | EMP004 仓储 | EMP005 主播 | EMP006 管理员 |
|---------|:----------:|:--------:|:--------:|:--------:|:--------:|:----------:|
| 数据总览（仪表盘）| ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| 直播监控 | ✅ | ✅ | ❌ | ❌ | ✅ | ✅ |
| 直播场次 | ✅ | ✅ | ❌ | ❌ | ❌ | ✅ |
| 选品分析 | ✅ | ✅ | ✅ | ❌ | ❌ | ✅ |
| 商品管理 | ✅ | ✅ | ✅ | ❌ | ❌ | ✅ |
| 主播管理 | ✅ | ✅ | ❌ | ❌ | ❌ | ✅ |
| 采购管理 | ✅ | ❌ | ✅ | ✅ | ❌ | ✅ |
| 库存管理 | ✅ | ❌ | ✅ | ✅ | ❌ | ✅ |
| 脚本管理 | ✅ | ✅ | ❌ | ❌ | ✅ | ✅ |
| 运营报告 | ✅ | ✅ | ❌ | ❌ | ❌ | ✅ |
| 售后工单 | ✅ | ✅ | ✅ | ❌ | ❌ | ✅ |

### 一、登录页 & 认证

- [ ] **正确密码登录** — 6 个账号分别能正常登录
- [ ] **错误密码登录** — 输入错误密码，应提示"用户名或密码错误"
- [ ] **空值校验** — 用户名或密码为空时点击登录，应有提示
- [ ] **Token 过期处理** — 长时间不操作后刷新页面，应自动跳回登录页
- [ ] **退出登录** — 点击退出后，应清除登录状态，无法再访问需要登录的页面
- [ ] **未登录直接访问** — 在未登录状态下直接访问 `http://localhost:5173/dashboard`，应跳转到登录页

### 二、仪表盘（数据总览）

> 所有 6 个角色都能访问，但数据权限可能不同。

- [ ] **KPI 卡片显示** — 四个指标卡片（GMV、订单量、转化率、库存预警）数据正常
- [ ] **数值格式** — GMV 显示为带单位的易读格式（如 ¥12.5 万），百分比保持合理精度
- [ ] **30 天销售趋势图** — 折线图正常渲染，数据点与鼠标悬停提示正确
- [ ] **TOP5 主播排行** — 显示销量前 5 的主播，排名数据合理
- [ ] **数据加载中状态** — 刷新页面时应有 loading 效果
- [ ] **数据为空处理** — 如果某个指标为 0 或 null，不报错

### 三、直播监控（实时模拟）

> 可访问角色：EMP001 / EMP002 / EMP005 / EMP006

- [ ] **进入监控页面** — 选择一个已存在的直播场次进入监控
- [ ] **在线人数实时变化** — 数字随时间波动（模拟真实直播）
- [ ] **订单实时推送** — 页面出现新订单通知
- [ ] **弹幕/聊天流** — 评论区持续滚动显示模拟弹幕，不卡顿
- [ ] **弹幕数量** — 检查弹幕是否持续出现，不会中断
- [ ] **情绪分析饼图** — 饼图更新反映弹幕情绪分布（正面/负面/中性）
- [ ] **AI 智能推荐** — 当检测到特殊弹幕信号时，右侧弹出 AI 推荐话术
- [ ] **启动/停止模拟** — 点击停止模拟后数据不再变化，重新启动后恢复
- [ ] **长时间运行** — 让监控页面运行 5 分钟以上，页面不崩溃、内存不泄漏

### 四、直播场次管理

> 可访问角色：EMP001 / EMP002 / EMP006

- [ ] **场次列表** — 正常显示已有场次，分页功能正常
- [ ] **创建新场次** — 选择主播、填写标题、设置时间和平台，提交后列表刷新
- [ ] **编辑场次** — 修改场次信息，保存后数据更新
- [ ] **删除场次** — 删除一个未开始的场次，确认弹窗正常
- [ ] **关联主播** — 创建场次时主播下拉列表数据正确
- [ ] **时间校验** — 结束时间早于开始时间应有提示

### 五、选品分析

> 可访问角色：EMP001 / EMP002 / EMP003 / EMP006

- [ ] **商品评分排行榜** — 表格显示多因子评分（转化率/利润率/热度/趋势/质量），排序正常
- [ ] **评分计算逻辑** — 检查几个商品的综合得分是否由各项加权计算而来
- [ ] **相似商品推荐** — 点击某个商品，右侧展示基于 Jaccard 相似度的推荐商品
- [ ] **推荐合理性** — 推荐的商品与源商品确实属于同一类别或相关类别
- [ ] **趋势分析** — 趋势图表数据正常渲染
- [ ] **AI 数字顾问报告** — 点击生成报告，内容包含合理的运营建议
- [ ] **新品冷启动评估** — 选择一个新品，点击冷启动评估，AI 返回分析结果
- [ ] **空数据** — 如果没有评分数据，页面不应报错

### 六、商品管理

> 可访问角色：EMP001 / EMP002 / EMP003 / EMP006

- [ ] **商品列表** — 分页加载正常，225 条商品均能查看
- [ ] **搜索功能** — 按商品名称搜索，模糊匹配结果正确
- [ ] **分类筛选** — 按商品类别筛选（数码、美妆、食品等），结果准确
- [ ] **状态筛选** — 按"在售/下架/缺货"状态过滤
- [ ] **排序功能** — 按价格、销量、利润率等字段排序，升序/降序切换正常
- [ ] **新增商品** — 填写完整信息（名称、分类、品牌、成本价、售价、供应商），创建成功
- [ ] **编辑商品** — 修改商品信息，保存后列表和数据刷新
- [ ] **删除商品** — 删除无关联数据的商品，有确认提示
- [ ] **删除保护** — 删除有关联订单/SKU 的商品时，后端应有错误提示
- [ ] **商品详情** — 点击商品可查看详情及关联 SKU
- [ ] **SKU 管理** — 可在商品详情中查看/管理 SKU
- [ ] **表单校验** — 价格为负数、必填字段为空时应有提示

### 七、主播管理

> 可访问角色：EMP001 / EMP002 / EMP006

- [ ] **主播列表** — 分页加载，25 位主播均能展示
- [ ] **搜索筛选** — 按主播名称、平台、等级筛选，结果正确
- [ ] **新增主播** — 创建新主播，填写姓名/性别/平台/粉丝数/专长/等级
- [ ] **编辑主播** — 修改主播信息后保存
- [ ] **删除主播** — 删除未关联场次的主播
- [ ] **主播详情** — 查看主播详细信息
- [ ] **雷达图** — 在主播详情中查看能力雷达图（转化率/观看时长/互动率/脚本执行分等维度）
- [ ] **多主播对比** — 选择多个主播进行雷达图叠加对比，图例区分清晰
- [ ] **主播绩效列表** — 绩效数据表格正常展示

### 八、脚本管理（AI 话术）

> 可访问角色：EMP001 / EMP002 / EMP005 / EMP006

- [ ] **脚本列表** — 517 条 AI 话术分页加载正常
- [ ] **查看脚本内容** — 脚本详情展示完整
- [ ] **AI 生成脚本** — 选择一个商品，点击生成，AI 返回直播话术
- [ ] **生成内容质量** — 生成的脚本内容通顺、与商品相关
- [ ] **预览后保存** — 生成后先预览，确认满意再保存到数据库
- [ ] **取消保存** — 预览后点取消，不会保存到数据库
- [ ] **手动创建脚本** — 手动填写脚本标题和内容，保存成功
- [ ] **编辑脚本** — 修改已有脚本
- [ ] **删除脚本** — 删除不需要的脚本
- [ ] **关联商品** — 脚本与商品关联正确

### 九、库存管理

> 可访问角色：EMP001 / EMP003 / EMP004 / EMP006

- [ ] **库存列表** — 多仓库库存记录加载正常
- [ ] **库存预警高亮** — 库存低于安全库存的记录有红色/黄色高亮标识
- [ ] **仓库筛选** — 按仓库名称筛选库存记录
- [ ] **修改库存** — 调整库存数量，保存生效
- [ ] **预警阈值** — 修改安全库存阈值，低于阈值的商品正确高亮
- [ ] **库存状态** — 各状态（正常/偏低/缺货）标签颜色正确

### 十、采购管理

> 可访问角色：EMP001 / EMP003 / EMP004 / EMP006

- [ ] **采购单列表** — 400 条采购单分页加载正常
- [ ] **采购状态流** — 检查状态流转：待审核 → 已审核 → 已发货 → 已收货
- [ ] **状态变更** — 每个状态变更操作后，采购单状态正确更新
- [ ] **状态流转限制** — 不能跳过中间状态（如从"待审核"直接到"已发货"）
- [ ] **创建采购单** — 选择供应商/SKU/数量/单价，提交成功
- [ ] **采购建议** — 页面显示低库存自动采购建议，有合理的建议数量
- [ ] **采购建议逻辑** — 建议确实针对库存偏低的 SKU
- [ ] **关联供应商** — 采购单与供应商关联正确
- [ ] **采购单详情** — 点击采购单查看明细

### 十一、售后管理

> 可访问角色：EMP001 / EMP002 / EMP003 / EMP006

- [ ] **售后工单列表** — 2000 条工单分页加载正常
- [ ] **工单筛选** — 按状态/类型/投诉等级筛选
- [ ] **工单详情** — 查看工单的问题描述、关联订单信息
- [ ] **状态处理** — 修改工单状态（待处理→处理中→已解决）
- [ ] **退款操作** — 执行退款操作后，退款金额记录正确
- [ ] **投诉等级** — 不同投诉等级的工单有清晰标识

### 十二、运营报告

> 可访问角色：EMP001 / EMP002 / EMP006

- [ ] **报告列表** — 20 条报告分页加载正常
- [ ] **报告分类** — 按报告类型筛选
- [ ] **报告详情** — 查看报告全文，内容包含数据分析和建议
- [ ] **报告生成** — 生成新报告（如果系统支持）

### 十三、订单管理（通过 API）

> 订单管理目前主要通过 API 查看

- [ ] **订单列表** — GET `/api/orders` 返回分页数据
- [ ] **订单状态** — 各状态（待付款/已付款/已发货/已完成/已取消）合理分布
- [ ] **订单金额** — 金额计算正确（原价 - 折扣 = 实际金额）

### 十四、权限隔离测试（重点！）

> 这是测试的核心——确保不同角色只能看到和操作自己权限范围内的内容。

- [ ] **EMP002 运营人员** — 登录后看不到"采购管理"和"库存管理"菜单
- [ ] **EMP003 采购人员** — 登录后看不到"直播监控""主播管理""脚本管理""运营报告"
- [ ] **EMP004 仓储人员** — 登录后只能看到"数据总览""采购管理""库存管理"
- [ ] **EMP005 主播** — 登录后只能看到"数据总览""直播监控""脚本管理"
- [ ] **URL 直接访问** — 用 EMP004 登录后，直接在地址栏输入 `/anchors`，系统如何处理？
- [ ] **API 直接调用** — 用受限账号的 Token 调用无权访问的 API，检查后端是否拒绝

### 十五、通用 UI/UX 测试

- [ ] **页面加载速度** — 每个页面首次加载在 3 秒以内
- [ ] **响应式布局** — 缩放浏览器窗口到不同宽度（1024px / 768px），布局不乱
- [ ] **侧边栏折叠** — 窗口缩小时侧边栏是否正确收起
- [ ] **分页功能** — 所有列表页的分页（上一页/下一页/跳转页码）正常工作
- [ ] **表格排序** — 支持排序的表格，点击表头正确排序
- [ ] **Loading 状态** — 数据加载时显示 loading 动画
- [ ] **空数据状态** — 搜索/筛选结果为空时，显示"暂无数据"提示，不报错
- [ ] **按钮防重复点击** — 提交按钮点击后应禁用，防止重复提交
- [ ] **操作成功提示** — 增删改操作成功后应有 toast/提示消息
- [ ] **操作失败提示** — 操作失败时应有错误提示，不静默失败
- [ ] **表单错误提示** — 表单字段校验失败时，错误信息清晰明了
- [ ] **浏览器兼容** — Chrome / Edge 浏览器均正常

### 十六、后端 API 测试

可使用 Postman 或浏览器开发者工具 Network 面板测试：

- [ ] **登录接口** — `POST /api/auth/login`，返回 token + 员工信息 + 角色 + 权限列表
- [ ] **Token 验证** — 不带 token 请求任何 `/api/*` 接口，返回 401
- [ ] **分页参数** — 带 `page` / `pageSize` 参数请求列表接口，分页数据正确
- [ ] **API 响应格式** — 所有接口返回 JSON，结构统一
- [ ] **错误处理** — 请求不存在的 ID 时返回合适的错误信息（非 500 崩溃）

### 十七、数据库数据完整性检查

在 SSMS 中执行以下检查：

- [ ] **24 张表全部存在** — 核对迁移是否完整
- [ ] **外键关联正确** — 抽查几张关联表（如订单→用户、订单→SKU），关联 ID 能查到对应记录
- [ ] **种子数据完整** — 主要表的数据量符合预期（见上文"执行时间说明"）
- [ ] **无脏数据** — 抽查：没有价格为负数的商品、没有空姓名的员工等

---

### 测试分工建议

如果多人协作测试，可按以下方式分工：

| 分工 | 负责内容 | 建议测试人 |
|------|---------|-----------|
| **分工 A** | 登录认证 + 仪表盘 + 权限隔离测试 | 测试人 1 |
| **分工 B** | 直播监控 + 直播场次 + 选品分析 | 测试人 2 |
| **分工 C** | 商品管理 + 主播管理 + 脚本管理 | 测试人 3 |
| **分工 D** | 库存管理 + 采购管理 + 售后管理 | 测试人 4 |
| **分工 E** | 运营报告 + 订单 API + 数据库完整性 | 测试人 5 |

每个分工至少用 2 个不同角色账号交叉验证。

---

### Bug 记录模板

发现 Bug 时，请按以下格式记录：

```
【Bug 编号】BUG-001
【发现人】张三
【测试账号】EMP002（运营人员）
【复现步骤】
  1. 登录系统
  2. 进入"xxx"页面
  3. 点击"xxx"按钮
【期望结果】应该正常显示 xxx
【实际结果】页面报错/白屏/数据不对，具体描述
【严重程度】致命 / 严重 / 一般 / 轻微
```

---

## 多人协作 Git 分支工作流

> 每个人在自己的分支上测试和改 Bug，互不干扰，最后合并到 main。

### 每个人的操作步骤（一看就会）

以下操作在项目文件夹的命令提示符里执行。

#### 第 1 步：创建你自己的分支

```bash
# 以"张三"为例，分支名用拼音，不要中文
git checkout -b test-zhangsan
```

分支命名建议：`test-你的名字拼音`

#### 第 2 步：正常测试 + 改代码

发现 Bug 直接改代码修复，也可以记录在测试文档里。

#### 第 3 步：提交你的修改

```bash
# 查看你改了什么
git status

# 添加你的修改（把下面的路径换成你实际改的文件）
git add .

# 提交，写清楚你做了什么
git commit -m "张三：完成商品管理模块测试，修复价格显示异常"

# 推送到 GitHub
git push -u origin test-zhangsan
```

> 第一次推送用 `git push -u origin test-zhangsan`，之后只需 `git push`。

#### 第 4 步：在 GitHub 上创建 Pull Request

1. 打开 https://github.com/N1ghts-9264/live-commerce-hub
2. GitHub 顶部会出现绿色的 "Compare & pull request" 按钮，点击
3. 写清楚你的 PR 标题和描述，例如：
   - 标题：`张三 - 商品管理 + 主播管理模块测试`
   - 描述：列出测试了哪些功能、发现了哪些 Bug、修复了什么
4. 点击 "Create pull request"

组长审核后合并到 main 分支即可。

---

### 分支管理速查

| 命令 | 说明 |
|------|------|
| `git checkout -b test-xxx` | 创建并切换到新分支 |
| `git checkout main` | 切回 main 分支 |
| `git branch` | 查看本地所有分支 |
| `git pull origin main` | 拉取 main 最新代码 |
| `git merge main` | 把 main 的更新合并到当前分支 |
| `git push origin test-xxx` | 推送到自己的远程分支 |
| `git branch -d test-xxx` | 删除本地分支（合并完成后） |

---

### 分工对应表

| 分工 | 负责模块 | 分支名 | 测试人 |
|------|---------|--------|--------|
| 分工 A | 登录认证 + 仪表盘 + 权限隔离 | `test-zhangsan` | 张三 |
| 分工 B | 直播监控 + 直播场次 + 选品分析 | `test-lisi` | 李四 |
| 分工 C | 商品管理 + 主播管理 + 脚本管理 | `test-wangwu` | 王五 |
| 分工 D | 库存管理 + 采购管理 + 售后管理 | `test-zhaoliu` | 赵六 |
| 分工 E | 运营报告 + 订单 API + 数据库完整性 | `test-sunqi` | 孙七 |

> 表格里的名字是示例，请换成你们实际的分工和分支名。

---

### 常见 Git 问题

**Q: 推送时报 "failed to push"**
```bash
# 先拉取远程更新再推
git pull origin main
git push origin test-你的分支名
```

**Q: 提交了不该提交的文件（如 .env）**
```bash
# 从 Git 追踪中移除（不删除文件）
git rm --cached server/.env
git commit -m "移除敏感文件"
```

**Q: 写错了 commit 信息（还没 push）**
```bash
git commit --amend -m "新的 commit 信息"
```

**Q: 不小心在 main 分支上改了代码**
```bash
# 把改动暂存，切换到自己的分支再应用
git stash
git checkout test-你的分支名
git stash pop
```

---

## 开发者信息

本项目为"信息系统分析与设计"课程项目，用于直播电商数字化运营管理系统的设计与实现。
